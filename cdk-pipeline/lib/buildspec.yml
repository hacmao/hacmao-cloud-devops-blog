version: 0.2
run-as: root

phases:
  install:
    commands:
      - npm install
  build:
    commands:
      - npm run build
      - pwd
      - ls
      - cp -r source/_posts/img/ themes/pure/source/
artifacts:
  base-directory: public
  files:
    - '*'